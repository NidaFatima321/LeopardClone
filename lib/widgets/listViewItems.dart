import 'package:flutter/material.dart';

class ListViewItems extends StatelessWidget {
  final String nic;
  final String category;
  const ListViewItems({Key? key, required this.nic, required this.category}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row(
      Text(&)
    )
      ],
    );
  }
}
